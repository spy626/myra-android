# LYRA Work — Minicoy website recovery checkpoint (2026-09-21)

**Pinned application source commit (do not substitute a later HEAD):** [`d18664675d23f82dafda7ea1b11e0812deb32997`](https://github.com/spy626/myra-android/commit/d18664675d23f82dafda7ea1b11e0812deb32997), built from `agent/myra-phase-1`. This documents the recovery *reference* for the user-selected APK. It is not an assertion that all of LYRA or other website prompts passed.

**APK identity:** byte-for-byte preserved copy of `app-debug.apk` in [Android CI run 35586233866](https://github.com/spy626/myra-android/actions/runs/35586233866), artifact ID `10632641810`, named `lyra-work-recovery-2026-09-21-d1866467.apk`. APK size `125105759` bytes, SHA-256 `a8ddc15e9729c1d0875f50d2a1539bfcd5ccd35c36172231ee6282510692057f`. CI ZIP digest SHA-256 `b5a0395a5b12a6ee4bf9e5ce33bd3a5b643535bf84545dca63bf4219f97c7771`; ZIP extraction matched the APK digest and APK ZIP integrity passed. CI unit tests and `assembleDebug` succeeded. The workflow's Android lint step reports existing repo findings separately/non-blockingly; **do not claim lint clean**. CI artifacts may expire, so keep a separate APK copy and this exact commit/checksum.

**Physical-phone evidence provided by user:** `Screen_Recording_20260921_154358.mp4` (~41.95 s). One Minicoy website prompt is submitted, shows `free attempt 1/3`, and the generated Preview loads without a visible broken image or horizontal overflow. The hero is blue, its heading white, and Explore CTA orange. `Things to Explore` is above the three compact pink Beaches/Lighthouse/Local Food cards. Before the CTA tap the status is absent; after tap, target outline and one `Exploring Minicoy!` status appear. Saved `index.html` shows a single `lyra-explore-feedback` node, native CTA `href="#lyra-explore-section"`, matching target heading, and three card headings. The editor shows `index.html`, `style.css` and empty `script.js` all Saved; native anchor/CSS is sufficient for this request. The saved CSS excerpt shows requested blue polish and native target rules. Chat shows a completion message and an Undo/Keep review control; the recording does **not** tap Undo/Keep. This is a specific website-task phone acceptance, not proof of all app behavior.

**Limits:** The video does not expose the installed APK's binary hash or signed package certificate, so it cannot independently prove the APK installed on the phone is bit-for-bit the pinned CI file. The observed task has no visible manual retry, but the recording cannot identify backend provider or hidden network attempts. Actual scroll *distance* is not measurable because the target is already onscreen; target activation and feedback are visible. Full CSS absence claims, long-session reliability, other prompts, all providers, and a new Undo/Keep test are outside this recording. The user separately reported Undo/Keep working in an earlier phone test. Do not publish the private screen recording.

**Recovery rule:** Preserve this exact APK copy and source SHA as the new *website-specific* fallback before further Work/provider changes. Keep the older [`74f603cd2f19bf9435e20020486d9f22d061474f`](https://github.com/spy626/myra-android/commit/74f603cd2f19bf9435e20020486d9f22d061474f) phone-verified recovery and required ancestor `c307357b53ec4ad3a005769d55f29bc44d74ff41` intact. Future code changes only on `agent/myra-phase-1`, never `main`, no force push. Rollback means use the pinned version only with appropriate data backup and compatible Android signing; never uninstall the current app or erase data just to test recovery. This document commits to the branch **after** the pinned application commit and does not change that APK. No GitHub tag or release is claimed.
