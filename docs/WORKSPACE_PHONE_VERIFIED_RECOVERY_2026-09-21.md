# LYRA Work — Build #2290 recovery checkpoint (2026-09-21)

**User-selected recovery = Build #2290, not a later build or branch HEAD.** Pin application source commit [`d18664675d23f82dafda7ea1b11e0812deb32997`](https://github.com/spy626/myra-android/commit/d18664675d23f82dafda7ea1b11e0812deb32997) from `agent/myra-phase-1` and [Build Android APK run #2290 (run ID 35586233866)](https://github.com/spy626/myra-android/actions/runs/35586233866). This is a website-specific recovery reference, not proof that all of LYRA or all website prompts passed. The documentation commit is NOT the recovery application commit.

**Exact recovery APK:** byte-for-byte copy of Build #2290's `app-debug.apk`, CI artifact ID `10632641810`, saved as `lyra-work-recovery-2026-09-21-d1866467.apk`. Size `125105759` bytes; SHA-256 `a8ddc15e9729c1d0875f50d2a1539bfcd5ccd35c36172231ee6282510692057f`. Original CI ZIP SHA-256 `b5a0395a5b12a6ee4bf9e5ce33bd3a5b643535bf84545dca63bf4219f97c7771`; extraction and APK integrity were checked. CI unit tests and `assembleDebug` passed; pre-existing Android lint findings are reported non-blockingly, so do not claim lint clean. CI artifact links can expire; preserve this separate exact APK copy and its checksum.

**Physical phone recording:** User-provided `Screen_Recording_20260921_154358.mp4` (~41.95 s). One Minicoy website prompt generated three saved files (`index.html`, `style.css`, empty `script.js`) and a working Preview without visible broken images or horizontal overflow. Blue hero, white Welcome heading, orange Explore CTA and three compact pink Beaches/Lighthouse/Local Food cards are visible, with Things to Explore heading above the cards. Before the tap the status is absent; after the tap the section target highlights and one Exploring Minicoy! status appears. Saved HTML shows one native feedback paragraph and matching CTA/heading anchor. The recording shows Undo/Keep controls but does not tap them.

**Separate user-confirmed phone acceptance:** The user explicitly confirmed that **both Undo and Keep work perfectly on the phone**, including in the current Build #2290 recovery discussion. Record this as user-reported physical-phone acceptance, distinct from what the recording itself visibly demonstrates; do not invent a recording, logs, or per-button timing. No Undo/Keep code was changed in the design-polish slice.

**Evidence limits:** Video does not expose installed APK SHA/signing certificate, so exact installed-binary identity cannot be independently proven from pixels. No visible manual retry, but backend provider and hidden network attempts are unknown. The target was onscreen before tapping, so measurable scroll distance remains unverified. Full CSS absence, longer-session reliability and unrelated prompts/providers are not covered. Do not publish the private recording.

**Recovery rules:** Keep Build #2290's exact APK and pinned source SHA as a website-specific fallback before Work/provider changes; retain older [`74f603cd2f19bf9435e20020486d9f22d061474f`](https://github.com/spy626/myra-android/commit/74f603cd2f19bf9435e20020486d9f22d061474f) recovery and required ancestor `c307357b53ec4ad3a005769d55f29bc44d74ff41`. Make future code edits only on `agent/myra-phase-1`; never modify `main` or force-push. Rollback requires an appropriate data backup and compatible APK signing; do not uninstall or erase data just to test recovery. This document is committed AFTER the pinned app commit and does not modify/rebuild the APK. No GitHub tag or release is claimed.
