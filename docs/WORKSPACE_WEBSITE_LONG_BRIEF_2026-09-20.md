# Website long brief acceptance — 2026-09-20

Phone evidence: the full new-chat Minicoy design request appeared in Chat but the Work website path returned `Website request exceeds 500 characters` before calling either free provider. This was LYRA's task-contract cap, not an OpenRouter/Groq quota or a website model refusal.

Fix on `agent/myra-phase-1` only: preserve the full website goal in the project task store and through restarts, project source snapshot and both free-provider request bodies. Distinguish website briefs (up to the same 64,000-character local Chat message capacity) from the existing 500-character Android one-file edit contract. Preserve paragraph breaks and normalize horizontal repeated whitespace only, without dropping requirements. The Task screen can view/edit the full website brief. Both free website routes use the same 96,000-character remote request budget, with preflight rejection of an oversized combined source + goal before networking, never silent truncation. Separate existing source screening, spec revision binding, provider consent, safe writes and Undo/Keep remain unchanged. Service availability and prompt adherence are not guaranteed by a size guard.

CI: `Website full-brief reconciled CI` ran the entire Android debug unit suite and assembled the debug APK, including tests for >500-character exact brief persistence through TaskStore recreation, full brief in OpenRouter and Groq request bodies, the 64k local boundary, unchanged Android 500-char contract, legacy approved initial goal matching and free-provider oversize refusal. The earlier staging CI failed three regressions and did not push; all were reconciled and the second workflow passed before the code commit. No phone acceptance has been claimed.
