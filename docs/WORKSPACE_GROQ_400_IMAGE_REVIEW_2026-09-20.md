# Workspace phone failure recovery — 2026-09-20

Scope: the user-tested Groq-only website build first returned HTTP 400 and an identical subsequent instruction worked; a generated three-card page displayed missing-image icons; a successful assistant reply was also repeated in the temporary status banner. The recording predates this recovery and cannot phone-accept it.

Source concepts: `docs/WORKSPACE_IDEA_LEDGER.md` (AIRI/Plast-Mem's source-owned task authority, spec-kit's separate verification, agent-skills mechanical QA and free-route ledger's bounded provider recovery). These are conceptual references only: no upstream code copied, no second router/model client/memory owner, no new dependency or permission. Groq's documented JSON Object Mode is the one-step compatibility mode; strict local three-file validation remains authoritative: https://console.groq.com/docs/structured-outputs . The precise provider-side cause of the initial HTTP 400 is unknown because the previous app intentionally did not expose response bodies.

Implemented on `agent/myra-phase-1` only: after a definitive Groq-primary HTTP 400, retry the exact task/source on Groq once using JSON Object Mode; never replay an uncertain timeout, quota 429 or the compatibility attempt. On success, omit newly invented `<img>` elements that have no pre-existing project markup; preserve existing matching markup, text cards, source freshness, explicit task scope and protected Undo/Keep. The assistant's saved Chat reply is the only terminal success text: clear the duplicate temporary banner. No network image generation or unapproved third-party source forwarding; no paid fallback. No guarantee of service availability or real-phone correctness.

Verification gates: Android unit tests cover HTTP-400 eligibility, identical approved context and free-model route for the compatibility request, strict local parser rejection, new-asset omission and existing-markup preservation. Debug APK compile passes on the one-time test-and-apply workflow; the separate exact-head Android CI plus release must pass before sharing an APK. New phone acceptance requires initial website generation (no manual repeat), successful cards without broken image icons, one Chat result, Preview inspection and Undo/Keep checks. User's accidentally exposed Groq key should be revoked separately; no key is included in this document.
