# LYRA Workspace Phase 5 slice 14 — explicit AI prompt handoff and single-JSON validation

Date: 2026-09-17. Base: `fd4bb19f0a9a58d1f0aa9512ce82d445ac27d45d` (Slice 13 recorded phone acceptance). Phase 5 remains OPEN. This slice still requires final-head CI, released debug APK and physical-phone acceptance before calling it phone-passed.

## Saved ideas reconciled

- `WORKSPACE_IDEA_LEDGER.md`: one AIRI/Plast-Mem owner and scoped PlanSpec authority; `github/spec-kit` task/criteria before implementation; bounded `context-mode`/Graft-style context; `microsoft/generative-ai-for-beginners` structured response; user CodeAI/SPCK chat-to-code loop. MERGED these concepts into the existing `WorkspaceSourceContext`, `WorkspaceStructuredEdit` and `WorkspaceScopedEdit`. No upstream repository code, runtime, model/client/router, database, permissions or paid dependency installed. The broader repository/feature rows remain explicitly deferred to their existing phases.
- User's Slice 13 double-JSON recording showed refusal without any file change, but an imprecise "old text missing" error. `WorkspaceStructuredEdit` now uses `JSONTokener` to require exactly one top-level JSON object and consume trailing input, so concatenated objects, arrays and trailing garbage are refused before any proposal or write. This is a single-document boundary, not a claim of an RFC-complete JSON grammar parser.
- `WorkspaceAiHandoff` prepares one bounded, locally reviewed prompt from the current user-selected eligible file and the existing approved/resumed task. It reuses the full-file sensitive-pattern scan and SHA-256 freshness checks, explicitly treats source text as untrusted JSON-quoted data, and asks for exactly the previously approved single-file `replace_exact_once` JSON shape. At most 1,500 source characters are included; if truncated, the prompt says so. Model-provided path, snippets, rationale and claims never gain authority.
- The existing structured-edit screen previews the entire prompt and offers a **separate opt-in Android clipboard copy** with a privacy warning; it rechecks source and saved spec before displaying copy confirmation and immediately before copying. Clipboard data may be readable by other apps, and pattern screening cannot guarantee secret detection; users must review the prompt and choose the recipient themselves. No automatic provider call, request metadata, key use, account registration, payment, route failover, source upload or persistent prompt storage is performed. Leaving the screen clears the in-memory prompt preview; OS clipboard remains under the user's control.
- The returned JSON remains untrusted and must pass existing path/spec/privacy/full-file fingerprint checks and a separate exact-write confirmation. The same Slice 12 private rollback and conflict-safe Undo/Keep path remains the only writer. There is no new permission to execute code, run shell commands or claim a build/browser verification.

## Verification and explicit limitations

- JVM tests for bounded local prompt/no write, changed-source/spec/paused/revoked denial, secret outside excerpt, pending backup denial, and rejecting concatenated JSON/trailing characters/array while preserving the file and rollback slot. The Android UI must remain usable on the user's phone; final-head CI and debug APK release are a prerequisite, not evidence of physical phone acceptance.
- Phone trial must use a harmless demo project: review/copy one-file prompt deliberately; paste it into a user-chosen safe destination (no need to create a paid API account), return one JSON response, preview, explicitly approve, and Undo. Test concatenated JSON rejection separately without file mutation. No real secrets should be copied.
- **This is user-mediated external-AI handoff, not native automatic AI coding.** Free-provider candidates in the ledger remain UNVERIFIED for exact current recurring-$0/no-card/no-AutoPay and privacy guarantees. Next work: audit an exact free route plus separate provider-send permission, one owned-model request with bounded response, checkpoint, and independent build/browser verification. No accidental claim that Phase 5 is finished.
