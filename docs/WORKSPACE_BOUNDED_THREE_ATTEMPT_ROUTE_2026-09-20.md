# LYRA Work website: bounded free-provider attempts

Phone failure: a website request started on OpenRouter, switched to Groq Free, and then stopped on Groq HTTP 400 even though Groq JSON Object Mode recovery existed for *Groq-primary* requests. Repeating the same user message sometimes succeeded; the previous recording is not a phone test of this change.

Implementation: preserve the one existing `WorkspaceChatCodingFlow` owner, one project snapshot and protected Undo. Count every actual website provider call, maximum **three total** (not three per provider). Current eligible sequence is OpenRouter Free initial → previously consented Groq Free with strict schema after a definitive OpenRouter 400/404/429/502/503/504 → Groq Free JSON Object Mode only after a definitive Groq-schema HTTP 400. A Groq-only initial request can use schema then JSON Object Mode, i.e., only **two** meaningful candidates. Do not re-send timeouts/unknown network outcomes, quota 429 to the same provider, authentication/payment errors, malformed model outputs, or a completed compatible attempt. No paid models, local/offline models, unverified providers, extra project-source forwarding permission, or unlimited loops. A saved Groq key is not alone website-source sharing consent; keep the existing website fallback + Free/ZDR settings. Do not expose credentials or model reasoning in logs.

Future candidate providers from `WORKSPACE_IDEA_LEDGER.md` are research only. When independently admitted (recurring $0 verified, no AutoPay or overage, privacy and exact model/schema capabilities), they may participate in a capability-checked, consented finite route plan under the same total-attempt cap. Do not imply the current app contains a dynamic N-provider router or a guaranteed third provider. Invalid/unsafe project writes remain rejected locally, and a saved assistant response must represent the final result once. New phone acceptance: send one website request once with two saved keys and existing website fallback enabled; observe OpenRouter rejection, Groq 400, third compatible request, actual three-file save and Preview, then test button and Keep/Undo. CI and JVM tests do not establish phone-pass or live provider success.
