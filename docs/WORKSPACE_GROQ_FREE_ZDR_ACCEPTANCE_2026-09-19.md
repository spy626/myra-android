# Groq Free + inference ZDR acceptance (2026-09-19)

User-confirmed: Groq account is on **Free tier**. User-provided screenshot of Groq Console > Settings > Data Controls shows **Inference APIs ZDR = Enabled** and **Global ZDR = Disabled**. Screenshot does not independently verify the billing tier, API key scope, whether settings have propagated, or model availability. Do not store or request the API key here.

Sources: https://console.groq.com/docs/your-data ; https://console.groq.com/docs/rate-limits ; https://console.groq.com/docs/deprecations ; https://console.groq.com/docs/model/openai/gpt-oss-120b ; https://console.groq.com/docs/billing-faqs . Groq's inference ZDR excludes input/output retention for reliability and abuse monitoring; usage metadata persists. GPT-OSS-120B is listed with a 131072-token native window, but Free-plan token-per-minute, daily and availability limits constrain practical context. Free Groq accounts do not need a payment method to stay Free; **Developer upgrades are billable**, and the Chat Completions API has no OpenRouter-style hard $0 pricing parameter. An account-level Free confirmation cannot be enforced from a stored API key. Accordingly, never promise Groq cannot bill if the account is later upgraded.

Implementation gate before Groq is activated: reuse the single Workspace gateway, secure existing Groq key, shared intent/context presentation, no Gemini Voice key, no extra memory/router/database. Explicit one-time Groq Free+ZDR account acknowledgement before opt-in; default OFF and local-only if not enabled; never route photos or source/attachments without a provider-specific disclosure, and never turn on other providers as fallbacks just because they have keys. Route only allowlisted *currently* supported Free-plan models, verify their native input/output and current Free quotas, track rate limits and enforce bounded retries. Preserve full user prompt and report limits rather than silently trimming; no `groq/compound` or remotely billed tools. When the account changes to Developer tier, disable Groq routing until a genuine provider-enforced $0 cap can be established (no app-level assertion can guarantee this). Tests must cover: privacy gating, key absence, full-text fidelity, no paid fallback, no cross-provider secret transfer, explicit opt-in, max tokens, no automatic edits, stop/cancel. Phone test and signed APK acceptance remain separate.

Status: user screenshot reviewed; provider route **not yet activated**. This entry documents evidence and cannot substitute for implementation/testing.